# Railway/Nixpacks — Java 21 + Maven build.

[phases.setup]
nixPkgs = ["maven", "jdk21"]

[phases.build]
cmds = ["./mvnw -B -DskipTests clean package"]

[start]
cmd = "java -Dserver.address=0.0.0.0 -Dserver.port=$PORT -jar target/admin-mydelivery-api-0.0.1-SNAPSHOT.jar"
